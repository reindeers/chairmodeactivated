Изначальное задание:

Допишите необходимый js, что бы код ниже выполнялся:


if (elements(by.tag('header'))
    .elements(by.css('.user-block'))
    .elements(by.attr('ng-model', 'email'))
    .first()
    .text() == 'a@x.com') {
           alert('Задание выполнено!');
    }


При условии, что на странице есть  такой фрагмент HTML


<header>
 <div class="user-block">
  <div ng-model="email">a@x.com</div>
 </div>
</header>


Что либо из внешних библиотек использовать нельзя.
Для поиска по DOM можно использовать document.querySelectorAll


______________________________________________________________


Примечание:
Если используется такая схема (абстрактно): 
получитьНесколькоЭлементовПоКлассу.получитьЕщеЭлементов
то из полученных по классу элементов для поиска возьмется только первый. В задании не стояло условие, что так не должно быть, поэтому 
с перебором не заморачивалась.
